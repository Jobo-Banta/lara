services:
  postgres:
    image: postgres:18
    environment:
      POSTGRES_DB: lara_local
      POSTGRES_USER: lara_migrator
      POSTGRES_PASSWORD: local-only-change-me
    ports: ["5432:5432"]
    volumes: [lara-postgres:/var/lib/postgresql]
volumes:
  lara-postgres:
