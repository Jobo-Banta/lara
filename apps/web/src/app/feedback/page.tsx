"use client";
import { useState } from "react";
export default function Page(){const [message,setMessage]=useState("");async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const f=new FormData(e.currentTarget);const r=await fetch("http://127.0.0.1:4000/demo/feedback",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({scenario:f.get("scenario"),route:f.get("route"),severity:f.get("severity"),message:f.get("message")})});const d=await r.json();setMessage(`Feedback captured as synthetic record ${d.feedbackId || ""}.`)}return <main className="workspace-page"><p className="eyebrow">Feedback · Demo</p><h1>Tell us where the journey needs work</h1><form className="demo-form" onSubmit={submit}><label>Scenario<input name="scenario" required defaultValue="DEMO-01"/></label><label>Route<input name="route" required defaultValue="/sales/invoices"/></label><label>Severity<select name="severity" defaultValue="minor"><option>minor</option><option>major</option><option>blocking</option></select></label><label>Feedback<textarea name="message" required rows={5}/></label><button className="primary-button">Save feedback</button></form>{message&&<p role="status" className="status neutral">{message}</p>}<a className="back-link" href="/">← Back to workspace</a></main>;}

