"use client";
import { useState } from "react";
export default function Page() { const [message,setMessage]=useState(""); async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();const form=new FormData(e.currentTarget);const r=await fetch("/api/demo/invoices",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({customer:form.get("customer"),subtotal:Number(form.get("subtotal")),idempotencyKey:`invoice-form-${form.get("customer")}`})});const data=await r.json();if(!r.ok){setMessage(data.error || "Unable to save. Sign in and retry.");return;}setMessage(`Saved synthetic invoice ${data.invoice?.id || ""}; total PHP ${data.invoice?.total || ""}`)} return <main className="workspace-page"><p className="eyebrow">Money in · Demo</p><h1>New invoice</h1><p className="page-intro">The 12% test tax fixture calculates a PHP 10,000 subtotal as PHP 11,200 total.</p><form className="demo-form" onSubmit={submit}><label>Customer<input name="customer" required defaultValue="Northwind Services"/></label><label>Subtotal<input name="subtotal" type="number" step="0.01" required defaultValue="10000"/></label><button className="primary-button">Save draft</button></form>{message&&<p role="status" className="status neutral">{message}</p>}<a className="back-link" href="/sales/invoices">← Invoice list</a></main>; }

