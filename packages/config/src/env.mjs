const MODES=new Set(["local","demo","staging","production"]);export function loadConfig(s=process.env){const mode=s.LARA_MODE;if(!MODES.has(mode))throw new Error("Invalid LARA_MODE");for(const n of ["APP_BASE_URL","API_INTERNAL_URL","DATABASE_URL","MIGRATION_DATABASE_URL","SESSION_SECRET_REF","OBJECT_ADAPTER","OBJECT_BUCKET","OBJECT_REGION","MAIL_ADAPTER","EINVOICE_ADAPTER","AI_ADAPTER","RULE_PROFILE_ID"])if(!s[n])throw new Error("Missing required configuration: "+n);if(mode==="production"&&(s.EINVOICE_ADAPTER==="fixture"||s.AI_ADAPTER==="fixture"||s.DEMO_RESET_ENABLED==="true"))throw new Error("Fixture adapters forbidden in production");if(mode!=="demo"&&s.DEMO_RESET_ENABLED==="true")throw new Error("Demo reset only allowed in demo");return Object.freeze({mode});}export function safeConfigError(e){return e instanceof Error?e.message.replace(/postgres(?:ql)?:\/\/[^\s]+/gi,"[redacted]"):"Invalid configuration";}