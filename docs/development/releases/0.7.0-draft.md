# Tax compliance and e-invoicing release 0.7.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.6.0 (P06) release, which is itself a draft.

- Release tag: _pending_ (`v0.7.0`).
- Exact-source CI: per ticket in the build tracker (P07-01..05 35497998831).
- Schema range: migration `0022` additive; the P06 images ignore the compliance tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` compliance journey; `scripts/test-p07-api.mjs`; `scripts/test-p07-domain.mjs` (P07-T01–T05) | passing in CI |
| RG-02 | P00–P06 suites unchanged; compliance negatives (self-approval of profiles, artifacts and returns, unmapped events, drifted artifact bytes, frozen lines after approval, filing without evidence, unsigned or wrong-taxpayer sends, expired profile, resend of an unknown outcome, retry of a financial rejection, pack as permit) | passing in CI |
| RG-03 | fresh and upgrade migration paths; approved artifacts, prepared lines of approved runs, filing records, payloads, attempts and decided cases immutable; restore rehearsal includes the compliance tables | passing in CI |
| RG-04 | defects fixed in-branch (blocked sends park instead of failing the job, superuser teardown of approved runs, queued → retry_wait). Open items are contract gaps: compliance profile, regulatory profiles, artifacts, registration submission and decision operations; a provisioned tax principal for the seeded `tax` template; CSV/PDF export of return lines | owner acceptance required |
| RG-05 | transmit, reconcile and pack jobs retry/dead-letter; `P07-RUNBOOK.md` with key custody and the rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | fixture transport only; provider adapter, sandbox certification, official artifacts and golden cases per enabled form, key and credential custody are external activation gates; no document leaves the system to an authority | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace`, `general_ledger`, `sales`, `purchasing`, `treasury`, `compliance` | pending owner run |

Activation: `compliance` per entity through capability activation with evidence and independent approval, then an approved `compliance_profile`, a regulatory profile from the authority's evidence approved and activated by different principals, one approved mapping artifact per enabled form with its golden case, and, for reporting, a certified provider adapter with the taxpayer's key and credentials in the worker environment. On rollback pause transmission, preserve the queue and reconcile remote status before resuming.
