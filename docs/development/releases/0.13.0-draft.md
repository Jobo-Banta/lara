# Customer and supplier portals and messaging release 0.13.0 — draft, not delivered

Prepared for the owner; no tag, deployment, channel or provider is attested. Depends on the 0.7.0 (P07) release — a draft — and on a qualified messaging channel and payment provider that do not exist in this release (local mail adapter and fixture provider only).

- Release tag: _pending_ (`v0.13.0`).
- Exact-source CI: GitHub run 35546146679 on commit addc5e6 (P13-01..05 done).
- Schema range: migration `0028` additive; the P12 images ignore the portal tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` portal journey; `scripts/test-p13-api.mjs`; `scripts/test-p13-domain.mjs` (P13-T01–T05) | passed in CI run 35546146679 |
| RG-02 | P00–P12 suites unchanged; portal negatives (capability and profile gates, wrong role for the party, invite beyond 30 days, drafter sending, self-authorization, wrong address, wrong token, second acceptance, member reaching another party by id or filter, member invoking internal operations, expired invite, expired or unknown share, supplier drafting a bill, link beyond the outstanding, wrong currency, second live intent, forged and stale signatures, premature and wrong-amount events, replays and repeats, cancelling a settled intent, append-only receipts) | passed in CI run 35546146679 |
| RG-03 | fresh and upgrade migration paths; receipts, intents once live, authorized messages and accepted invites immutable; restore rehearsal includes the portal tables | passed in CI run 35546146679 |
| RG-04 | defects fixed in-branch (authorized→failed on a relay failure, accepted invites revocable, lazy expiry, minimized subjects, worker grant for token rotation, provider receipts on a reviewed treasury bank account). Open items: portal profile operation; relations and authorizer role templates; hosted channel and payment provider; PDF statements; dunning levels | owner acceptance required |
| RG-05 | authorized sends are jobs with retries, a receipt per attempt and dead-letter tasks; webhooks answer 200 with a recorded verdict; `P13-RUNBOOK.md` with the rollback rule (revoke grants, pause sends) | runbook written |
| RG-06 | browser journey covers refusal, denial, empty, loading and revoked states with WCAG checks for the member and the team; task-based acceptance not yet run | pending |
| RG-07 | qualified channel and payment provider, consent and notification policy, terms and support recorded in the profile; provider evidence absent — messaging and links ship disabled without it | enforced in code (profile gate), acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `ai_assistance`, `portals` | pending owner run |

Activation: `portals` per entity through capability activation with evidence and independent approval, then an approved `portal_profile`; without a qualified channel and provider the profile names `none` and the portal ships read-only. On rollback revoke the invites and pause sends; receipts, intents and the event log stay readable.
