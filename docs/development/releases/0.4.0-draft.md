# Sales invoicing and receivables release 0.4.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.3.0 (P03) release, which is itself a draft.

- Release tag: _pending_ (`v0.4.0`).
- Exact-source CI: per ticket in the build tracker (P04-01 35398987989, P04-02 35401224779, P04-03 35402118677, P04-04/05 _pending_).
- Schema range: migrations `0016`–`0018` additive; the P03 images ignore the sales tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` sales journey; `scripts/test-p04-api.mjs`; `scripts/test-p04-domain.mjs` (P04-T01–T05); `tests/domain.test.mjs` kernel rounding | passing in CI |
| RG-02 | P00–P03 suites unchanged; sales negatives (client totals, control-account lines, inactive rule, self-approval, over-credit, double allocation, cross-currency and cross-party allocation, exhausted or recycled numbers) | passing in CI |
| RG-03 | fresh and upgrade migration paths; issued documents, number events, tax events, allocation events and party snapshots immutable; restore rehearsal includes the sales tables | passing in CI |
| RG-04 | defects fixed in-branch (settlement state enumeration, credit allocation source, allocation intents). Open items are contract gaps: sales profile and numbering series operations, payment terms, credit limits, delivery listing, per-line tax in `DocumentResource`, sales order approval permission in a seeded role | owner acceptance required |
| RG-05 | `scripts/test-p02-load.mjs` issuance profile (unique numbers under concurrency); delivery and aging jobs retry/dead-letter; `P04-RUNBOOK.md` | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | `sales_profile.reportingRequired` blocks issuance for reporting-required customer profiles until P07; sample rates never activated | enforced |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace`, `general_ledger`, `sales`; delivery adapter is local (qualified adapter is an activation gate) | pending owner run |

Activation: `sales` per entity through capability activation with evidence and independent approval, then an approved `sales_profile`, numbering series per branch and at least one tax rule version approved and activated by different principals from cited evidence and golden cases. Official reporting-required profiles stay blocked until P07.
