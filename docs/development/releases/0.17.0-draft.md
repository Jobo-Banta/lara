# Local operations feature releases 0.17.0 (P17A–P17E) — draft, not delivered

Prepared for the owner; no tag or deployment is attested. Each feature ships independently behind its own capability and profile; the phase is complete only when the features the owner selects pass their gates. Depends on the 0.7.0, 0.9.0, 0.11.0 and 0.16.0 releases (drafts) as specified per feature.

- Release tag: _pending_ (`v0.17.0`; feature tags `v0.17.0-a` … `-e` if shipped separately).
- Exact-source CI: per ticket in the build tracker (P17A–E-01..05 _pending_).
- Schema range: migration `0032` additive with the five capabilities and twenty-two permissions; the P16 images ignore the new tables. Existing source-owner boundaries transfer only at a signed cutoff; opening imports per feature are reviewed.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` local operations journey; `scripts/test-p17-api.mjs`; `scripts/test-p17-domain.mjs` (P17-T01–T05) | passing locally, CI pending |
| RG-02 | P00–P16 suites unchanged; feature negatives (capability and profile gates, escalation outside the term, deposit beyond the contract, self-application, application beyond what is held, second billing of a period, ineligible and expired customers, re-discounting a posted invoice, unexplained payout nets, replayed statements, posting before reconciliation, sales reconciled twice, untying payroll totals, replayed files, unauthorized record reads, remittances not tying, obligations outside the authority profile, completion without filing evidence) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; approved leases and eligibilities immutable, events and records append-only, posted payouts and batches immutable; restore rehearsal includes the new tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (non-cash settlement for deposit application through the allocation model, masked identity references, per-read audit of payroll records). Open items: role templates; lessee accounting; provider statement adapters; payroll integrations beyond file import; 2316 rendering | owner acceptance required |
| RG-05 | replays refused by source hash and reference; posting once per payout, batch, source and period; `P17-RUNBOOK.md` with the rollback rule (disable the feature's capability; reconcile or cancel pending payouts and batches) | runbook written |
| RG-06 | browser journey covers the five screens with approval, exception and evidence states and WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | per-feature authority, provider and accounting examples and source formats reviewed before activation; without them the feature stays disabled | pending owner review |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `planning`, `leases`, `statutory_discounts`, `marketplace_pos`, `payroll_data`, `local_obligations` | pending owner run |

Activation: each feature per entity through capability activation with evidence and independent approval, then its approved profile. On rollback disable the feature's capability; posted entries and documents remain.
