# Financial institution coexistence release 0.8.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.7.0 (P07) release, which is itself a draft.

- Release tag: _pending_ (`v0.8.0`).
- Exact-source CI: per ticket in the build tracker (P08-01..05 35503479657).
- Schema range: migration `0023` additive; the P07 images ignore the institution tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` institution journey; `scripts/test-p08-api.mjs`; `scripts/test-p08-domain.mjs` (P08-T01–T05) | passing in CI |
| RG-02 | P00–P07 suites unchanged; institution negatives (self-approval of ownership and mappings, overlapping windows, ingestion without ownership, unapproved mapping, checksum conflict, manifest count/total/hash mismatch, unmapped account, unbalanced atomic batch, foreign currency, frozen rows after approval, immutable posted batches and facts, examiner writes) | passing in CI |
| RG-03 | fresh and upgrade migration paths; approved ownership and mappings, posted batches and their rows, balance snapshots, instrument facts and roll-ups immutable; restore rehearsal includes the institution tables | passing in CI |
| RG-04 | defects fixed in-branch (feed errors persisted for the workbench instead of rolled back, `created_by` on instrument facts). Open items are contract gaps: source systems, mapping import and approval, expected batches, profile and waivers; an institution-officer role template; feed adapters; FR-FI-003 wording | owner acceptance required |
| RG-05 | report and sweep jobs retry/dead-letter; `P08-RUNBOOK.md` with the watermark rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | design partner's signed matrix and parallel month, source system owners and cutoffs, tax advisor's rule versions and golden cases are external activation gates; no adapter pulls from a bank; core banking stays authoritative | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `compliance`, `fi_coexistence` | pending owner run |

Activation: `fi_coexistence` per entity through capability activation with the signed feed matrix and independent approval, then an approved `fi_profile`, registered source systems, approved ownership windows per book and family, one approved mapping version per system, and the reviewed GRT/DST/withholding rule versions with golden cases. On rollback stop source acceptance at the durable watermark and reconcile in-flight batches before resuming.
