# Evidence-backed AI assistance release 0.12.0 — draft, not delivered

Prepared for the owner; no tag, deployment, provider or activation is attested. Depends on the 0.7.0 (P07) release — a draft — and on a hosted provider adapter that does not exist in this release.

- Release tag: _pending_ (`v0.12.0`).
- Exact-source CI: GitHub run 35530924780 on commit d0eb62e (P12-01..05 done).
- Schema range: migration `0027` additive; the P11 images ignore the AI tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` assistant journey; `scripts/test-p12-api.mjs`; `scripts/test-p12-domain.mjs` (P12-T01–T05) | passing (CI 35530924780) |
| RG-02 | P00–P11 suites unchanged; assistant negatives (capability off, feature off, budget spent, unknown or foreign evidence and records, requester without report permission, self-approval of enablement and of evaluation acceptance, failed evaluation, fewer than 200 items, model change while enabled, injected tools, blank scan, timeout, uncertain accept without reason, review twice, revoked requester) | passing (CI 35530924780) |
| RG-03 | fresh and upgrade migration paths; completed runs, reviewed suggestions and evaluations immutable; restore rehearsal includes the AI tables | passing (CI 35530924780) |
| RG-04 | defects fixed in-branch (multiple tool phrases per line, audit entity for tenant-level configuration, evaluation set upsert). Open items are contract gaps: configuration, evaluation sets and results, acceptance; requester and reviewer role templates; hosted provider adapter and real held-out evaluation; OCR | owner acceptance required |
| RG-05 | runs are jobs with retries and dead-letter tasks; timeouts and budgets fail closed; `P12-RUNBOOK.md` with the rollback rule (feature or model version only) | runbook written |
| RG-06 | browser journey covers refusal, denial, abstention, empty and loading states with WCAG checks; task-based acceptance and review-time measurement not yet run | pending |
| RG-07 | privacy/security approval of provider terms, Finance's evaluation thresholds and acceptance, Product's review-time measurement | enforced in code (evaluation gate, acceptance, opt-out), acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `assets`, `ai_assistance` | pending owner run |

Activation: `ai_assistance` per entity through capability activation with evidence and independent approval, then the provider policy, the held-out evaluation accepted by Finance and the feature switched on by another principal. On rollback switch the feature off or reconfigure the previous model version; accounting records are untouched.
