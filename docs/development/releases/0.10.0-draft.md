# Inventory costing and three-way matching release 0.10.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.6.0 (P06) release and, for foreign-currency items, the 0.9.0 (P09) release — both drafts.

- Release tag: _pending_ (`v0.10.0`).
- Exact-source CI: per ticket in the build tracker (P10-01..05 _pending_).
- Schema range: migration `0025` additive; the P09 images ignore the inventory tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` inventory journey; `scripts/test-p10-api.mjs`; `scripts/test-p10-domain.mjs` (P10-T01–T05, AC-09) | passing locally, CI pending |
| RG-02 | P00–P09 suites unchanged; inventory negatives (receipt without cost, self-approval, frozen lines, concurrent issue beyond stock, serial in two warehouses, serial quantity, cost method change after activity, unclassified allocation method, backdated issue, locked period, bill for goods without a receipt, variance beyond tolerance without disposition, posting twice) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; posted movements, layers, allocations, approved counts and runs immutable; restore rehearsal includes the inventory tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (unit cost text scale, half-up allocation rounding, `created_by` on lots, count movement state sequence). Open items are contract gaps: warehouses, inventory profile, opening stock import; stock clerk and approver role templates; item weights and unit conversions; price variance clearing; FX items | owner acceptance required |
| RG-05 | valuation report job retries/dead-letters; postings synchronous; `P10-RUNBOOK.md` with the rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | controller's signed costing method, unit conversions, cutoff, negative-stock policy and opening count; stock/GL/AR/AP and inventory outputs on the parallel month | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `multi_currency`, `inventory` | pending owner run |

Activation: `inventory` per entity through capability activation with evidence and independent approval, then an approved `inventory_profile`, warehouses per branch, items with their cost method, and the opening count reconciled to the ledger opening. On rollback deactivate the capability; movements stop and the valuation evidence stays readable.
