# Accounting firm multi-client workspace release 0.14.0 — draft, not delivered

Prepared for the owner; no tag or deployment is attested. Depends on the 0.7.0 (P07) and 0.13.0 (P13) releases — both drafts — and on each client signing the permission and expiry boundaries of its mandate.

- Release tag: _pending_ (`v0.14.0`).
- Exact-source CI: per ticket in the build tracker (P14-01..05 _pending_).
- Schema range: migration `0029` additive; the P13 images ignore the firm tables. No existing membership changes until a client approves a mandate; no affiliation is inferred from domains or addresses.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` firm journey; `scripts/test-p14-api.mjs`; `scripts/test-p14-domain.mjs` (P14-T01–T05) | passing locally, CI pending |
| RG-02 | P00–P13 suites unchanged; firm negatives (second firm in an organization, staff enrolled by staff, unknown roles, security authorities in a mandate, a second live mandate per firm, the drafter approving, widening or extending an approved mandate, an assignment wider than the mandate or by staff, a delegate reaching an entity or operation outside its subset, two client tenants without an explicit context, a delegate granting or approving a mandate, revoked mandates reopened, aggregates and snapshots for an unheld scope, the firm invalidating a client's snapshots, cross-tenant reads and deletes) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; mandates and assignments never deleted by the runtime, approved mandates never widened, revoked mandates final; restore rehearsal includes the firm tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (forced row security on the restricted functions, firm directory for id-only discovery, firm's own entity for the audit trail, receipt against the firm record for bulk reminders). Open items: client switcher persistence beyond the session, CSV/PDF exports of the roll-up, deadline calendars, reviewed role templates for partners and mandate approvers | owner acceptance required |
| RG-05 | bulk reminders run per client under a savepoint with independent outcomes; snapshots are deleted by mandate on revocation; sessions and queued jobs fail their revocation recheck; `P14-RUNBOOK.md` with the rollback rule (disable the capability in the firm, keep every direct client scope) | runbook written |
| RG-06 | browser journey covers the firm registration, the two-principal mandate, the labelled roll-up, the explicit client context, refusal outside the subset, revocation and the empty state with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | each client signs permission and expiry boundaries (mandate evidence, second approver, expiry within a year); adversarial counts and cross-client jobs covered by the schema and domain suites | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `portals`, `firm_workspace` | pending owner run |

Activation: `firm_workspace` per entity through capability activation with evidence and independent approval, in the firm's organization and in each client. The firm registers once; a client grants a mandate on the engagement evidence, a second principal approves it, and the firm's partner assigns staff subsets that never exceed it. On rollback disable the capability in the firm: aggregation stops, every direct client scope and the client audit trail remain.
