# General ledger release 0.3.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.2.0 (P02) release, which is itself a draft.

- Release tag: _pending_ (`v0.3.0`).
- Exact-source CI: per ticket in the build tracker (P03-01 35391416551, P03-02 35394107803, P03-03 35394822049, P03-04/05 _pending_).
- Schema range: migrations `0012`–`0015` additive; the P02 images ignore the ledger tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` ledger journey; `scripts/test-p03-api.mjs`; `scripts/test-p03-domain.mjs` (P03-T01–T05) | passing in CI |
| RG-02 | P00–P02 suites unchanged; posting negatives (unbalanced, duplicate source, locked period, control account, frozen, non-leaf, missing dimension) | passing in CI |
| RG-03 | fresh and upgrade migration paths; readiness requires `0015`; posted facts immutable | passing in CI |
| RG-04 | defects fixed in-branch (date shifting across zones, closing into locked period, dimension rules on closings). Open items are contract gaps: ledger line drill-down, import row listing, fiscal-year close operation | owner acceptance required |
| RG-05 | restore rehearsal covers ledger tables; report jobs retry/dead-letter; `P03-RUNBOOK.md` | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states; task-based acceptance not yet run | pending |
| RG-07 | not applicable to enabled scope; `ledger_profile` settings gate reopening | n/a |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace`, `general_ledger` | pending owner run |

Activation: `general_ledger` per entity through capability activation with evidence and independent approval; the primary PHP book opens on activation. Statutory CAS replacement remains out of scope until P07.
