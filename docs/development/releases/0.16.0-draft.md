# Budgets, cost allocation and project accounting release 0.16.0 — draft, not delivered

Prepared for the owner; no tag or deployment is attested. Depends on the 0.10.0 (P10) and 0.11.0 (P11) releases — drafts — and on finance's approval of the budget basis, drivers, project and retention profiles and contract evidence for each entity.

- Release tag: _pending_ (`v0.16.0`).
- Exact-source CI: per ticket in the build tracker (P16-01..05 _pending_).
- Schema range: migration `0031` additive; the P15 images ignore the planning tables. Budgets and project openings start from reviewed entries with no retroactive reservation; historical transactions are tagged only through dimensions.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` planning journey; `scripts/test-p16-api.mjs`; `scripts/test-p16-domain.mjs` (P16-T01–T05) | passing locally, CI pending |
| RG-02 | P00–P15 suites unchanged; planning negatives (capability gate, foreign-currency budgets, duplicate buckets, author approving, approved budgets edited, two active budgets, blocked orders without override authority or reason, consumption reduced, zero driver totals, control accounts allocated, stale previews, reruns, suppliers as customers, certification beyond the milestone or by the owner, billing beyond certification or contract, recoupment beyond the advance, retention released twice, contract changed in place) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; approved budgets and rules immutable, posted runs and billings immutable, consumption append-only; restore rehearsal includes the planning tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (bucket lock for simultaneous reservations, home tenant for multi-tenant identities on the shell, override reason on the order approval). Open items: role templates for the planner and project manager; dimension picker; taxable progress billing; view exports | owner acceptance required |
| RG-05 | reservation, consumption and release ride the purchasing operations; allocations post once with a stale-preview guard; billings apply on invoice posting; `P16-RUNBOOK.md` with the rollback rule (disable the capability, release by cancelling orders, correct by versions, reversals and credit notes) | runbook written |
| RG-06 | browser journey covers activation, budget versions, availability, the blocked order and override, consumption, allocation preview to posting, project contract, certification, advance, progress billing, retention, release and change order with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | finance approvals: budget approval and activation, rule approval, project profile, contract version approval, certification on evidence | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `group_accounting`, `planning` | pending owner run |

Activation: `planning` per entity through capability activation with evidence and independent approval, then the approved project profile, budget versions and rule versions. On rollback disable the capability: no new reservations, runs or billings; posted entries and documents remain.
