# Treasury cash and bank reconciliation release 0.6.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.5.0 (P05) release, which is itself a draft.

- Release tag: _pending_ (`v0.6.0`).
- Exact-source CI: per ticket in the build tracker (recorded when the runs complete).
- Schema range: migration `0021` additive; the P05 images ignore the treasury tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` treasury journey; `scripts/test-p06-api.mjs`; `scripts/test-p06-domain.mjs` (P06-T01–T05) | pending CI |
| RG-02 | P00–P05 suites unchanged; treasury negatives (control-account bank, self-approval of accounts, matches and transfers, unbalanced statement, repeated line keys, fee inside a match, overlapping confirmations, receipt posted before clearing, double clearing, variance without reason, cashier self-handover, second release, direct API channel) | pending CI |
| RG-03 | fresh and upgrade migration paths; statement lines, allocations, checks in circulation, posted transfers, closed sessions, counts, handovers and file runs immutable; restore rehearsal includes the treasury tables | pending CI |
| RG-04 | defects fixed in-branch (statement imports without balanced totals, cleared checks dishonorable, deterministic line ordering). Open items are contract gaps: treasury profile, statement batches, bank file runs, count lines and handover attestations; variance in `CashSessionResource`; transfer approver and second handover principal in a seeded role; bank account on every settlement | owner acceptance required |
| RG-05 | statement and propose jobs retry/dead-letter; `P06-RUNBOOK.md` with the rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | pilot bank format `lara-csv-1` needs bank and customer acceptance, signer matrix and custody controls; direct bank API disabled; no operation sends money | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace`, `general_ledger`, `sales`, `purchasing`, `treasury` | pending owner run |

Activation: `treasury` per entity through capability activation with evidence and independent approval, then an approved `treasury_profile`, bank accounts entered from the bank's confirmation and approved by a different principal, and a signed opening statement cutoff reconciled to the ledger before matching begins. On rollback stop new release files and reconcile released or unknown payments first.
