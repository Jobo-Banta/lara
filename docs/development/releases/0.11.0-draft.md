# Assets, recurring work and recognition schedules release 0.11.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.5.0 (P05) release and, for foreign-currency purchases, the 0.9.0 (P09) release — both drafts.

- Release tag: _pending_ (`v0.11.0`).
- Exact-source CI: per ticket in the build tracker (P11-01..05 _pending_).
- Schema range: migration `0026` additive; the P10 images ignore the asset and schedule tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` assets journey; `scripts/test-p11-api.mjs`; `scripts/test-p11-domain.mjs` (P11-T01–T05, AC-08) | passing locally, CI pending |
| RG-02 | P00–P10 suites unchanged; asset negatives (cost beyond the bill, bill without a clearing line, self-approval, approved asset edited, unapproved policy, wrong end date, draft schedule run, pending version holds the run, rerun, locked period, event inside a posted period, event without available evidence, disposed asset events, CIP depreciation, basis beyond the deferral line, immutable posted lines and layers) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; events, components, reviewed versions, executed lines and layers immutable; restore rehearsal includes the asset tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (exact declining-balance rate, pending planned lines counted in a prospective opening, draft schedules ended as rejected on disposal, split children draw nothing from the bill). Open items are contract gaps: classes, asset profile, policies, opening register import; asset clerk and approver role templates; custodian screen; separate tax book posting | owner acceptance required |
| RG-05 | schedule runs are jobs with retries and dead-letter tasks; capitalization and event postings synchronous; `P11-RUNBOOK.md` with the rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | controller's approved classes, recognition methods, opening register, impairment and revaluation journals and disclosure boundary; scheduled posting authority explicit and revocable | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `inventory`, `assets` | pending owner run |

Activation: `assets` per entity through capability activation with evidence and independent approval, then an approved `asset_profile`, the classes, the recognition policies and the opening register reconciled to the ledger. On rollback pause every schedule and reconcile the last committed period; deactivate the capability and the register stays readable.
