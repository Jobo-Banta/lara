# Multiple currencies and separate books release 0.9.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.8.0 (P08) release, which is itself a draft.

- Release tag: _pending_ (`v0.9.0`).
- Exact-source CI: per ticket in the build tracker (P09-01..05 _pending_).
- Schema range: migration `0024` additive (existing entries backfilled at rate 1; posted values unchanged); the P08 images ignore the FX tables and the new posting-function arguments.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` multi-currency journey; `scripts/test-p09-api.mjs`; `scripts/test-p09-domain.mjs` (P09-T01–T05, AC-10) | passing locally, CI pending |
| RG-02 | P00–P08 suites unchanged; FX negatives (inverted pair, out-of-line value, self-approval, immutable approved rates, posting without a rate, unbalanced or mismatched functional sides, management book posting, closed partition, unclassified accounts, non-closing rate, second run per rate set, posting twice) | passing locally, CI pending |
| RG-03 | fresh and upgrade migration paths; approved rates, layers, posted runs and rated entries immutable; restore rehearsal includes the FX tables | passing locally, CI pending |
| RG-04 | defects fixed in-branch (functional header totals, residual on the control line, reversal adjustments in the carrying value, update grants on links). Open items are contract gaps: FX profile, book access grants, book links, currency metadata; FX desk and approver role templates; rate feed adapter; cross-currency settlements; functional-currency migration procedure; treasury foreign-currency accounts | owner acceptance required |
| RG-05 | postings are synchronous; `P09-RUNBOOK.md` with the rollback rule | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | controller's approval of the monetary classification, FX sources, functional currencies and book boundaries, tested separate-book reports; no rate feed | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `fi_coexistence`, `multi_currency` | pending owner run |

Activation: `multi_currency` per entity through capability activation with evidence and independent approval, then an approved `fx_profile`, reviewed rates from published sources, and separate books activated by a principal other than their creator with access grants recorded. On rollback deactivate the capability; native-currency history and reports stay readable.
