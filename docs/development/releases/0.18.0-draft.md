# Controlled extensibility feature releases 0.18.0 (P18A–P18D) — draft, not delivered

Prepared for the owner; no tag or deployment is attested. Each feature ships independently behind its own capability; the phase is complete only when the features the owner selects pass their gates. Depends on the 0.12.0 and 0.14.0–0.17.0 releases (drafts) as applicable per feature. Peer benchmarking and government withholding are not activated by this release.

- Release tag: _pending_ (`v0.18.0`; feature tags `v0.18.0-a` … `-d` if shipped separately).
- Exact-source CI: GitHub run 35584400992 on commit db2cf74 (P18A–D-01..05 done).
- Schema range: migration `0033` additive with the four capabilities and six permissions; the P17 images ignore the new tables. Pack catalog fixtures ship under `packages/packs/`.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` extensibility journey; `scripts/test-p18-api.mjs`; `scripts/test-p18-domain.mjs` (P18-T01–T05) | passed in CI run 35584400992 |
| RG-02 | P00–P17 suites unchanged; feature negatives (capability gates, unlisted fields and joins, unselected sort, over-budget definitions, injected filter values, drafts run, author publishing, edits after publication, entities outside the scope, metrics without their permission, reserved custom-field keys; unknown golden cases, approval before or against a failing impact, self-approval, stale content versions; grants to members, entities outside the granter's scope, unapproved grants, widening, ungranted and non-allowlisted tools, injected instructions, entities outside the grant, rate limit, the client reading the log, requests after revocation; tampered manifest hashes, unsupported upgrade paths, reinstalling, installing without pack authority) | passed in CI run 35584400992 |
| RG-03 | fresh and upgrade migration paths; published definitions, approved proposals and grants and frozen manifests immutable, runs append-only; restore rehearsal includes the new tables | passed in CI run 35584400992 |
| RG-04 | defects fixed in-branch (budget-row locking for commitments, the publish action's request body, the pack rollback path for superseded versions). Open items: Security/Finance acceptance of the allowlists and packs; role templates; non-VAT golden cases; export path | owner acceptance required |
| RG-05 | parameterized compilation only; scope on every row and aggregate; denials recorded; revocation immediate; `P18-RUNBOOK.md` with the rollback rule (disable the capability, revoke grants, retire definitions, roll packs back; no financial fact changes) | runbook written |
| RG-06 | browser journey covers the four screens with refusal, denial, empty and evidence states and WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | report catalog, tool allowlist and per-pack manifests and tests accepted by Security/Finance before activation; without acceptance the feature stays disabled | pending owner review |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `local_obligations`, `report_authoring`, `rule_proposals`, `client_tools`, `industry_packs` | pending owner run |

Activation: each feature per entity through capability activation with evidence and independent approval after the owner's acceptance of its allowlist or catalog. On rollback disable the feature's capability, revoke grants and roll back pack versions; posted entries, documents, runs and the audit trail remain.
