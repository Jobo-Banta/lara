# Purchasing, payables and expenses release 0.5.0 — draft, not delivered

Prepared for the owner; no tag, deployment or activation is attested. Depends on the 0.4.0 (P04) release, which is itself a draft.

- Release tag: _pending_ (`v0.5.0`).
- Exact-source CI: per ticket in the build tracker (P05-01/02/03 35478773366; P05-04/05 35480573825).
- Schema range: migrations `0019`–`0020` additive; the P04 images ignore the purchasing tables.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` purchasing journey; `scripts/test-p05-api.mjs`; `scripts/test-p05-domain.mjs` (P05-T01–T05) | passing in CI |
| RG-02 | P00–P04 suites unchanged; purchasing negatives (client totals, revenue or control-account lines, missing supplier reference, duplicate reference, two-way match excess, missing receipt of service, self-approval, over-credit, withholding on an accrued bill, payment authority by the preparer, release without authority or evidence, settlement before release, return of an advance, liquidation beyond the advance, cross-family reads) | passing in CI |
| RG-03 | fresh and upgrade migration paths; posted bills, receipts of service, reviewed beneficiaries, released and settled payment orders, advance events, tax events and issued certificates immutable; restore rehearsal includes the purchasing tables | passing in CI |
| RG-04 | defects fixed in-branch (supplier credits on the payable side, settled payments returnable, concurrent duplicate translated to `DUPLICATE_SOURCE`). Open items are contract gaps: purchasing profile, bill series, beneficiary, receipt-of-service, advance and certificate operations; withholding in `DocumentResource`; per-supplier withholding on the party; purchaser permissions in a seeded role | owner acceptance required |
| RG-05 | `scripts/test-p02-load.mjs` bill profile (unique numbers and one bill per supplier reference under concurrency); aging job retry/dead-letter; `P05-RUNBOOK.md` | runbook written |
| RG-06 | browser journey covers refusal, denial, empty and loading states with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | withholding timing is profile-selected and recognized once; certificates derive from tax events with a reviewed output version; bank-file and API releases refused until P06; no operation sends money | enforced |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace`, `general_ledger`, `sales`, `purchasing`; manual payment release only | pending owner run |

Activation: `purchasing` per entity through capability activation with evidence and independent approval, then an approved `purchasing_profile` (control accounts, withholding timing, PO and receipt-of-service policy, supplier withholding codes signed by finance), optional `bill` numbering series per branch, and one approved beneficiary version per payee entered and reviewed by different principals from the bank verification document. Finance signs the manual payment responsibility; certificate output beyond the reviewed JSON version waits for P07 or a signed external preparation boundary.
