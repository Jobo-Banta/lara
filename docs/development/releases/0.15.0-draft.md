# Intercompany and consolidation release 0.15.0 — draft, not delivered

Prepared for the owner; no tag or deployment is attested. Depends on the 0.9.0 (P09) and 0.11.0 (P11) releases — drafts — and on the controller's approval of ownership, method, account mappings, rate policies and statement presentation for each group.

- Release tag: _pending_ (`v0.15.0`).
- Exact-source CI: GitHub run 35546146679 on commit addc5e6 (P15-01..05 done).
- Schema range: migration `0030` additive; the P14 images ignore the group tables. Entity keys are retained; the existing branch roll-up is not converted to intercompany; no ownership is assumed for prior periods.

| Gate | Evidence | Status |
| --- | --- | --- |
| RG-01 | `tests/browser/workspace.spec.mjs` group journey; `scripts/test-p15-api.mjs`; `scripts/test-p15-domain.mjs` (P15-T01–T05) | passed in CI run 35546146679 |
| RG-02 | P00–P14 suites unchanged; group negatives (capability gate, minority interests and the equity method, reporting entity missing, duplicate mappings, author approving a mapping or rate set, activation before an approved mapping or by the definer, active group changed, pair raised outside the source entity or with the wrong counterpart kind or currency, second pair per document, posting before acceptance, source entity accepting, reviewer accepting or posting, accepter approving its own bill, posted pairs edited, runs without frozen closes, unapproved rate sets or mappings, missing member snapshots, approval before preview or by the preparer, stale content versions, unresolved differences, approved inputs changed, cross-entity reads) | passed in CI run 35546146679 |
| RG-03 | fresh and upgrade migration paths; approved mappings and rate sets immutable, issued pair sides never rolled back, approved runs frozen, one published run per period; restore rehearsal includes the group tables | passed in CI run 35546146679 |
| RG-04 | defects fixed in-branch (acceptance prepares the target document without granting the accepter approval, a side that posts clears the exception it caused, prior-period earnings default to the group retained earnings line, reciprocal balances taken from open items at the cutoff). Open items: role templates for the group accountant and reviewer; partial ownership, equity method and transaction-rate income translation; worksheet exports | owner acceptance required |
| RG-05 | pair posting is a saga with an exception state and reason, never an automatic reversal; runs are deterministic and re-previewable; `P15-RUNBOOK.md` with the rollback rule (disable the capability, supersede rather than delete, member ledgers untouched) | runbook written |
| RG-06 | browser journey covers activation, definition, mapping approval, rate set, pair acceptance, the exception state, posting, readiness, preview, approval and publication with WCAG checks; task-based acceptance not yet run | pending |
| RG-07 | controller approval of ownership and method (group activation), mappings (mapping approval), rate policies (rate set approval); minority or equity-method members are refused | enforced in code, acceptance pending |
| RG-08 | `scripts/release-manifest.mjs` from the exact-source run; capabilities `workspace` … `firm_workspace`, `group_accounting` | pending owner run |

Activation: `group_accounting` per reporting entity through capability activation with evidence and independent approval, then a group with an approved mapping version and activation by a second principal, and an approved rate set per period end. On rollback disable the capability: published snapshots stay readable and member ledgers are unchanged.
